import { createContext } from "react";


  export const PassingContext = createContext<any>('');
  export const Pass2Context = createContext<any>('');
  export const Pass3Context = createContext<any>('');

  export const Guard1Context = createContext<any>('');
  export const Guard2Context = createContext<any>('');
  export const Guard3Context = createContext<any>('');

  export const Takedown1Context = createContext<any>('');
  export const Takedown2Context = createContext<any>('');
  export const Takedown3Context = createContext<any>('');

